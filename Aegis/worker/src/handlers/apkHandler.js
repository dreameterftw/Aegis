export async function handleApk(body, db, env) {
  // Phase 2 fills this in: call Render /analyze, run Groq verdict, writeCache
  return Response.json({ status: 'not_implemented', module: 'apk' }, { status: 501 });
}
