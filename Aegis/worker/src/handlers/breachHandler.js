export async function handleBreach(body, db, env) {
  // Phase 4 fills this in: hash-prefix lookup against breach_index
  return Response.json({ status: 'not_implemented', module: 'breach' }, { status: 501 });
}
